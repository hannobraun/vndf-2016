Tasks:
- trait Entity
- trait World<E: Entity>
- World::import_entity(entity: E)

trait Entity {
	??? (maybe id())
}

trait World<E: Entity> {
	// new/from_entities are static and thus not possible to specify here?


	fn import_entity(&mut self, entity: E);
	fn destroy_entity(&mut self, id: EntityId);
	fn to_entities(&self) -> Vec<E>;
}
