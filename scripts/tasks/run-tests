#!/usr/bin/env bash

source project.conf

export LD_LIBRARY_PATH=$(dirname $RUSTC)/../lib
export PATH=$(pwd)/source/rust/target:$(dirname $RUSTC):$(dirname $CARGO):$PATH
export RUST_TEST_TASKS=1

(
	cd source/rust
	cargo build) &&
./scripts/tools/cargo-test \
	$RUSTC \
	$CARGO \
	source/rust
