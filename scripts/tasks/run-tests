#!/usr/bin/env bash

source project.conf

export PATH=$RUST_PATH:$PATH
export RUST_TEST_TASKS=1

./scripts/tasks/build-vndf &&
(
	export RUST_LOG=vndf_server=info
	export RUST_BACKTRACE=1
	cd $TEST_SUITE_SOURCE
	cargo test)
