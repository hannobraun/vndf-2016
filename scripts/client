BINARY_DIR=output/bin
BINARY=$BINARY_DIR/vndf-client

mkdir -p $BINARY_DIR

clang source/client/main.c output/lib/libglfw.a \
	-lGL -Isource/include/ -o $BINARY \
	-Werror -Weverything -Wno-unused-parameter &&
	./$BINARY
